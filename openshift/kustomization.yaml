apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Suite-level entry point. Add future components below as they ship.
resources:
  - shared/namespace.yaml
  - collector
  # - api          # added when riptide-api ships
  # - dashboard    # added when riptide-dashboard ships

namespace: riptide
